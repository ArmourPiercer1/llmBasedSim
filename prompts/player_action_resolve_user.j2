## 玩家行动
{{ player_action }}

{% if rule_result %}
## 系统规则预判
{{ rule_result }}

系统规则预判是基于结构化数据得出的约束判断。除非当前场景中存在明确、具体、且优先级更高的反例，否则你应以该预判为准。
如果你调整或反驳该预判，必须在 `feasibility_reason` 中说明具体原因。
{% endif %}

## 玩家能力
{{ capabilities }}

## 玩家物理数据
{{ physical_profile }}

## 当前环境
{{ environment }}

## 场景中的物体
{% if objects %}
{% for obj_id, obj in objects.items() %}
- [{{ obj_id }}] {{ obj.name }}：{{ obj.description }}
  位置：{{ obj.position }}
  状态：{{ obj.state if obj.state else "正常" }}
  属性：{{ obj.properties if obj.properties else {} }}
{% endfor %}
{% else %}
（当前没有物体信息）
{% endif %}

## 地点信息
{% if locations %}
{% for loc_id, loc in locations.items() %}
- [{{ loc_id }}] {{ loc.name }}：{{ loc.description }}
{% endfor %}
{% else %}
（当前没有地点信息）
{% endif %}

## 任务
判断玩家行动在当前能力和物理条件下是否可行，并返回更新后的同一个玩家行动结构。